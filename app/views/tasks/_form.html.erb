<%= semantic_form_for @task do |f| %>
  <%= f.input :title, required: true %>
  <%= f.input :priority, as: :radio, collection: [1,2,3], required: true %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :button %>
  <% end %>
<% end %>
